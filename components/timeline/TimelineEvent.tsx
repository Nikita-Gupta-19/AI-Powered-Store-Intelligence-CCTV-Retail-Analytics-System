'use client';import { motion } from 'framer-motion';export default function TimelineEvent({event}:any){return <motion.div initial={{opacity:0,x:-10}} animate={{opacity:1,x:0}} className='relative pl-8'><span className='absolute left-0 top-2 h-3 w-3 rounded-full bg-blue-400'/><p className='font-mono text-xs text-blue-300'>{event.time}</p><p className='text-sm text-slate-200'>{event.label}</p><p className='text-xs text-slate-500'>Confidence {Math.round(event.confidence*100)}%</p></motion.div>}
