import Link from 'next/link';import { Card } from '@/components/ui/card';import { Badge } from '@/components/ui/badge';import { formatDate } from '@/lib/utils';export default function IncidentCard({incident}:any){return <Link href={`/dashboard/incidents/${incident.id}`}><Card className='hover:border-blue-500/50'><div className='flex justify-between gap-3'><h3 className='font-semibold'>{incident.title}</h3><Badge variant={incident.severity==='CRITICAL'?'danger':'warning'}>{incident.severity}</Badge></div><p className='mt-2 line-clamp-2 text-sm text-slate-400'>{incident.description}</p><p className='mt-3 text-xs text-slate-500'>{incident.location?.name||'Delhi NCR'} • {formatDate(incident.createdAt)}</p></Card></Link>}
