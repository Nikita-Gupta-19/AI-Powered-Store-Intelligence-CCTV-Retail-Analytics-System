'use client';import { motion } from 'framer-motion';import { Card } from '@/components/ui/card';export default function StatCard({label,value,trend}:{label:string;value:string|number;trend?:string}){return <motion.div whileHover={{y:-4}}><Card><p className='text-sm text-slate-400'>{label}</p><div className='mt-2 text-3xl font-bold'>{value}</div>{trend&&<p className='mt-1 text-xs text-green-300'>{trend}</p>}</Card></motion.div>}
