'use client';import { Card } from '@/components/ui/card';export default function MediaViewer({urls=[]}:any){const files=Array.isArray(urls)?urls:JSON.parse(urls||'[]');return <div className='grid gap-4 md:grid-cols-2'>{files.length?files.map((u:string)=><Card key={u}>{u.match(/\.mp4|\.webm|video/)?<video controls src={u} className='w-full rounded-xl'/>:<img src={u} alt='uploaded evidence' className='h-64 w-full rounded-xl object-cover'/>}</Card>):<Card>No uploaded media</Card>}</div>}
